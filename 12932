class Solution {
    public int[] solution(long n) {
        String str = String.valueOf(n);
        int[] answer = new int[str.length()];
        int num = str.length();

        for(int i=0;i<num;i++) {
            answer[i]= str.charAt(num-1-i)-'0';

        }
        return answer;
    }
}
